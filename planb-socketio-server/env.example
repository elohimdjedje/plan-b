# Port du serveur Socket.io
PORT=3001

# URL du frontend (pour CORS)
FRONTEND_URL=http://localhost:5173

# Passphrase JWT (optionnel, pour vérification complète)
# Doit correspondre à JWT_PASSPHRASE dans planb-backend/.env
JWT_PASSPHRASE=

# Note: Pour une vérification complète en production, 
# utilisez la clé publique JWT depuis config/jwt/public.pem

# URL du backend Symfony (pour vérifications)
SYMFONY_URL=http://localhost:8000

